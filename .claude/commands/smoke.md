Run a quick smoke test of the dashboard using browser automation (claude-in-chrome MCP).

Steps:
1. Open http://localhost:8000 in a new browser tab.
2. Verify the page loads and the title contains "AI Co-Host".
3. Check that all 6 panels are visible: Control Panel, Ask ChatGPT, Configuration, Context Summary, Live Conversation, AI Insights.
4. Check the nav bar status indicators (Discord, OpenAI, Voice, Latency, Connection, Health).
5. Take a screenshot and report results to the user.
6. Close the tab when done.
